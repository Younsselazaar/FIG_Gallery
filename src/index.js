import { AppRegistry } from "react-native";
import App from "./App";

/**
 * React Native entry point
 * ------------------------
 * MUST match:
 * - MainActivity.getMainComponentName()
 * - Android app/build.gradle (entryFile)
 */

AppRegistry.registerComponent("FigGallery", () => App);
